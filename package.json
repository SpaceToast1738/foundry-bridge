{
  "name": "foundry-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "bundle": "npm run build:bundle --workspaces --if-present",
    "dist": "npm run build && npm run bundle && node scripts/check-dist.mjs",
    "test": "jest --passWithNoTests",
    "lint": "eslint .",
    "clean": "tsc --build --clean && rimraf \"packages/*/build\" \"packages/*/dist\""
  },
  "dependencies": {
    "playwright": "^1.60.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.0.0",
    "eslint": "^9.39.2",
    "globals": "^17.0.0",
    "jest": "^29.7.0",
    "jiti": "^2.6.1",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.2.5",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.51.0"
  },
  "engines": {
    "node": ">=20"
  }
}
