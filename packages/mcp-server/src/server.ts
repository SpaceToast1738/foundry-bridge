#!/usr/bin/env node
import { PACKAGE } from "@foundry-bridge/shared";

// Server skeleton. Real entry point implemented in later tasks.
console.error(`${PACKAGE}: mcp-server skeleton not yet implemented`);
process.exit(0);
